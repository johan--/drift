/**
 * Violation type definitions
 * 
 * Re-exports from rules module for convenience.
 */

export type {
  Violation,
  QuickFix,
  WorkspaceEdit,
  TextEdit,
  Range,
  Position,
  FixType,
} from '../rules/types.js';
